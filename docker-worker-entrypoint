#!/usr/bin/env sh
#set -Eeuo pipefail

i=0
until [ $i -gt 3 ]
do
  echo Check migrate finished...
  if [ ! -e migrate-ok ]; then
    if [ $i -ge 3 ]; then
      exit 1
    fi

    sleep 5
    i=$((i=i+1))
  else
    break
  fi
done

exec /var/www/html/yii mtm-worker
